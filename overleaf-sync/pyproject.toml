[project]
name = "overleaf-local-sync"
version = "0.1.0"
description = "Local-first sync helpers (GUI + CLI wrapper) for a self-hosted Overleaf instance."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

